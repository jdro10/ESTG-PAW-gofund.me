<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Active campaigns</title>
  <link rel="stylesheet" type="text/css" href="/stylesheets/activeCampaignsCSS.css">
  <link href="https://fonts.googleapis.com/css?family=Luckiest+Guy&display=swap" rel="stylesheet">
</head>
<body>
  <div class="topFooter">
    <h1 class="title">ACTIVE CAMPAIGNS</h1>
  </div>

  <form action="/loginPage" method="get">
   <button type="submit" class="back">Back to menu</button> 
 </form>

 <% if(campanhas.length > 0) { %>
 <table border="1" class="customers">
  <thead>
    <tr>
      <th>Description</th>
      <th>Target value</th>
      <th>IBAN</th>
      <th>Responsibles</th>
      <th>Title</th>
      <th>Type</th>
      <th>State</th>
    </tr>
  </thead>
  <tbody>
    <% for(var i = 0; i < campanhas.length; i++) { %>
    <% if(campanhas[i].estado === 'Ativo'){ %>
    <tr>
      <td><a href="/campaign/show/<%= campanhas[i]._id %>"><%= campanhas[i].descricao %></a></td>
      <td><%= campanhas[i].valorAtingir %> €</td>
      <td><%= campanhas[i].iban %></td>
      <td><%= campanhas[i].responsaveis %></td>
      <td><%= campanhas[i].title %></td>
      <td><%= campanhas[i].type %></td>
      <td><%= campanhas[i].estado %></td>
    </tr>
    <% } %>
    <% } %>
  </tbody>
</table>
<% }else{ %>
<h1>There are no active campaigns</h1>
<% } %>    
</body>
</html>